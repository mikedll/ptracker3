<!DOCTYPE html>
<html>
  <head>
    <title>Reactexperiments</title>
    <%= csrf_meta_tags %>
	  <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag    'bootstrap-lib', media: 'all' %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= javascript_pack_tag 'application' %>

    <%= yield :head %>
  </head>

  <body>
    <div class="container-fluid">

      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="<%= root_path %>">Product Management</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item <%= link_active('welcome') %>">
              <a class="nav-link" href="<%= root_path %>">Home</a>
            </li>
            <li class="nav-item <%= link_active('welcome') %>">
              <a class="nav-link" href="<%= root_path %>">Purchase Orders</a>
            </li>
            <li class="nav-item <%= link_active('items') %>">
              <a class="nav-link" href="<%= items_path %>">Items</a>
            </li>
          </ul>
        </div>
      </nav>

      <%= yield %>
    </div>
  </body>
</html>
